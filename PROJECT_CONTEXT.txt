PROJECT CONTEXT (carbon69)

GOAL
- Build a public-facing website for Carbon69, an auto parts company.
- Provide product discovery (search/browse), product detail pages, and a cart flow (checkout later).
- Support internal catalog management (admin tools and APIs).

CURRENT STATE SUMMARY
- Backend: Django REST API with catalog, cart, and order endpoints, custom User model, JWT auth, and MinIO-backed media/file storage.
- Frontend: Vite + React + TypeScript + Tailwind storefront with home, catalog, product, cart, login, and orders screens.
- Infra: docker-compose with Django (gunicorn), nginx, Postgres, Redis, Celery worker/beat, MinIO.

BACKEND DETAILS
- Location: backend/
- Stack: Django 6.0, djangorestframework 3.16.1, simplejwt 5.5.1, Celery 5.4, boto3
- Apps:
  - api: Public catalog endpoints, cart and order APIs, catalog-page aggregate endpoint, and MinIO presigned upload/download URLs.
  - catalog/cart/orders: Domain models for storefront and checkout flow.
  - users: Custom User model with role (admin/manager) and role-based permission helpers.
- Auth: JWT; default permission is IsAuthenticated (hello and file endpoints are AllowAny).
- Storage: MinIO client for presigned PUT/GET; bucket created at startup (minio_setup.py).
- DB: Postgres via env or sqlite fallback.

FRONTEND DETAILS
- Location: frontend/
- Stack: Vite + React + TypeScript + Tailwind CSS v3.
- App: URL-based SPA-like navigation (history API) for /, /catalog, /product/:id, /cart, /login, /orders.
- Catalog UI: server-driven category tree, product cards, and filter controls wired to backend query params.
- Orders UX: authenticated users can create orders from cart and view order history.

ROUTING / SERVICES
- API:
  - /api/hello/
  - /api/catalog-page/
  - /api/categories/, /api/categories/main/
  - /api/brands/, /api/banners/, /api/products/
  - /api/carts/, /api/cart-items/
  - /api/orders/
  - /api/files/presign-upload/
  - /api/files/{id}/presign-download/
  - /api/token/, /api/token/refresh/
- Docker: nginx proxies /api and /admin; serves built frontend.

DEV COMMANDS
- Backend: cd backend; .\.venv\Scripts\Activate.ps1; python manage.py runserver
- Frontend: cd frontend; npm install; npm run dev
- Docker: docker compose up --build

GAPS FOR CARBON69 GOALS
- No online payment provider integration yet (planned separately).
- Shipping carrier integration is model-level only; no shipping API/UI workflow yet.
- No dedicated customer profile/registration/password recovery flows yet.
- Search/filter UX is present but can be expanded (sort modes, numeric ranges, URL query persistence).

NEXT STEP IDEAS
- Define core domain models: Product, Category, Brand, Fitment, Inventory, Price.
- Build catalog APIs (list, search, detail) and admin workflows.
- Design site pages: Home, Catalog, Product Detail, About, Contact, Cart.
- Decide ecommerce scope: guest checkout vs accounts, payments, tax/shipping.
